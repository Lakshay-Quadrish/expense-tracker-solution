import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antigravity.expensetracker',
  appName: 'My Expense Tracker',
  webDir: 'www'
};

export default config;
